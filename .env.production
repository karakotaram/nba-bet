# Live draft server (Railway). Public URL — not a secret. CRA inlines
# REACT_APP_* vars into the production build (npm run build / npm run deploy).
REACT_APP_DRAFT_SERVER_URL=https://nba-bet-production.up.railway.app
